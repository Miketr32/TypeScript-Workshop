function sumar(a: number, b: number): number {
    return a + b
} //que pasa si yo quisiera concatenar strings con esta funcion?

function SumaString(a: string, b:number ): string {
    return a + b
}

console.log(SumaString('3', 2));